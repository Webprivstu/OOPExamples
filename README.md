Currently working on quiz system using object orientated programming in university but it is currently in the planning stages so there is no code (UML diagram).

The library example should be functional, the Smart Device Controller is an example for a user class in a larger project using inheritance and abstraction.

Here are two basic OOP examples that should cover some major fundamentals and will likely be included in the final project.
